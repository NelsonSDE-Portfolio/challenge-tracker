[build]
  command = "npm run build"
  publish = "dist"

# Edge function for CORS headers
[[edge_functions]]
  path = "/assets/*"
  function = "cors"

[[edge_functions]]
  path = "/*.js"
  function = "cors"

# Edge function for OG meta tags on shared workouts
[[edge_functions]]
  path = "/share/*"
  function = "share-og"

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
